# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# macOS system file
*.DS_Store

# Documentation source files
*.docx

# Log and trajectory files
*.log
*_md.log
*.traj

# Output files from simulations
*_params.txt
*_parameters.txt
*_convergence.csv
*_convergence.png
*_energy.csv
*_energy.png
*_temperature.png
*_pressure.png
*_volume.png
neb_data.csv
neb_energy.png
md_energy.csv
md_energy.png
md_temperature.png
md_pressure.png
md_volume.png
md.traj
opt.traj
opt_final.vasp

# NEB output directories (numbered POSCAR folders)
tests/fixtures/structures/**/[0-9][0-9]/


# Virtual environments (README Quick Start creates .venv in the repo)
.venv/
venv/
.venv-*/

# Packaging and build artifacts
*.egg-info/
build/
dist/

# Pytest & coverage
.pytest_cache/
.coverage
# parallel = true writes one data file per process (.coverage.<host>.<pid>.<n>),
# including one per committee worker subprocess, before `coverage combine`.
.coverage.*
.cache/
coverage.xml

# VS Code or other IDE settings
.vscode/

# CLI or temp command data
command_cache.*
command_history.*

# Claude Code local harness state (settings.local.json, env.json, logs).
# No shared project config lives here yet; if a tracked .claude/settings.json
# is ever added, switch this to ".claude/*" + "!.claude/settings.json".
.claude/
